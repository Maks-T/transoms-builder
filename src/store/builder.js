import { defineStore } from 'pinia';
import { LEAF_TYPES, PROFILE_TYPE } from './constants';

// Хранилище для управления состоянием конструктора
export const useBuilderStore = defineStore('builder', {}
}